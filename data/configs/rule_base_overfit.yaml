rule_base:
  dataset_names:
  - dl3dv-overfit
  image_transform_args:
    image_stride: 16
    max_image_size: 1024
    min_image_size: 512
  vit_image_transform_args:
    image_stride: 14
    max_image_size: 518
    min_image_size: 224
  enable_vit_condition: false
  add_context_role_text: true
  is_mandatory: false
  shuffle_lines: True
  shuffle_seed: 0
  num_used_data:
  - 100
  weight: 1
